# EasyNote 前端环境变量配置示例
# 使用方法：复制此文件并重命名为 .env.local，然后根据实际情况修改

# 后端 API 基础地址 (FastAPI 默认端口为 8000)
VITE_API_BASE_URL=http://localhost:8000/api

# 注意事项：
# 1. 生产环境下，请将 localhost 替换为您的后端实际域名或 IP。
# 2. AI API Key 等敏感信息已全部移至后端配置，前端不再需要直接持有。

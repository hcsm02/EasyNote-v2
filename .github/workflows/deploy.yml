name: Deploy EasyNote

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Extract metadata (tags, labels)
        id: meta
        run: |
          # 强制将镜像名称转换为小写
          echo "IMAGE_REPO=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: 🛠️ Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_REPO }}:latest
            ${{ env.IMAGE_REPO }}:${{ github.sha }}
          no-cache: true

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          command_timeout: 20m
          script: |
            # 简化版部署逻辑：仅拉取镜像并重启，排除网络干扰
            IMAGE_REPO=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
            IMAGE_TAG=${{ github.sha }}
            
            echo "📥 正在极速拉取新版本镜像: $IMAGE_TAG"
            docker pull $IMAGE_REPO:$IMAGE_TAG
            
            echo "🔄 正在秒速重启服务..."
            docker stop easynote || true
            docker rm easynote || true
            
            docker run -d \
              --name easynote \
              -p 8038:8000 \
              -v /aiModels/docker/EasyNote/data:/app/data \
              --env-file /aiModels/docker/EasyNote/.env \
              --restart unless-stopped \
              $IMAGE_REPO:$IMAGE_TAG
            
            # 后台清理，不影响启动速度
            docker image prune -f
            
            echo "🔍 检查容器状态..."
            docker ps -a | grep easynote
            
            echo "📋 正在获取容器最后 20 行日志..."
            docker logs --tail 20 easynote
            
            echo "✅ 部署指令执行完毕！SHA: $IMAGE_TAG"
            docker inspect --format='创建时间: {{.Created}}' easynote
            
            echo "✅ EasyNote 已启动。请访问: http://120.24.168.78:8038"

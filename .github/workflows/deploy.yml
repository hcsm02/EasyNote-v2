name: Deploy EasyNote

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Extract metadata (tags, labels)
        id: meta
        run: |
          # 强制将镜像名称转换为小写
          echo "IMAGE_REPO=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: 🛠️ Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_REPO }}:latest
            ${{ env.IMAGE_REPO }}:${{ github.sha }}
          no-cache: true

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          command_timeout: 20m
          script: |
            # 极速部署逻辑：国内加速镜像 + 源码物理挂载
            IMAGE_REPO=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
            IMAGE_TAG=${{ github.sha }}
            MIRROR_REPO="ghcr.nju.edu.cn/$(echo "${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')"
            
            cd /aiModels/docker/EasyNote
            
            # 1. 物理层 Git 同步 (极速)
            git pull origin main
            
            # 2. 尝试从南京大学镜像源拉取 (更快)
            echo "📥 正在拉取镜像环境..."
            docker pull $MIRROR_REPO:$IMAGE_TAG || docker pull $IMAGE_REPO:$IMAGE_TAG
            
            echo "🔄 正在秒速重启服务 (源码已同步)..."
            docker stop easynote || true
            docker rm easynote || true
            
            # 关键：精准挂载。只挂载代码子目录，不遮挡镜像里的 static 文件夹（前端）
            docker run -d \
              --name easynote \
              -p 8038:8000 \
              -v /aiModels/docker/EasyNote/data:/app/data \
              -v /aiModels/docker/EasyNote/backend/main.py:/app/main.py \
              -v /aiModels/docker/EasyNote/backend/routers:/app/routers \
              -v /aiModels/docker/EasyNote/backend/models:/app/models \
              -v /aiModels/docker/EasyNote/backend/database.py:/app/database.py \
              -v /aiModels/docker/EasyNote/backend/config.py:/app/config.py \
              -v /aiModels/docker/EasyNote/backend/utils:/app/utils \
              -v /aiModels/docker/EasyNote/backend/schemas:/app/schemas \
              -v /aiModels/docker/EasyNote/backend/services:/app/services \
              --env-file /aiModels/docker/EasyNote/.env \
              --restart unless-stopped \
              $IMAGE_REPO:$IMAGE_TAG
            
            docker image prune -f
            
            echo "✅ 挂载已精细化！网页和后端现在均已就绪。"
            docker logs --tail 20 easynote

name: Deploy EasyNote

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Extract metadata (tags, labels)
        id: meta
        run: |
          # 强制将镜像名称转换为小写
          echo "IMAGE_REPO=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: 🛠️ Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_REPO }}:latest
          # 确保不使用旧缓存
          no-cache: true

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          command_timeout: 20m  # 将 SSH 命令超时增加到 20 分钟
          script: |
            # 定义变量（转换为小写以匹配 GHCR 标准）
            IMAGE_REPO=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
            
            cd /aiModels/docker/EasyNote
            
            # 更新本地代码（用于获取最新的 Dockerfile 或配置）
            git pull origin main
            
            # 1. 先尝试拉取新镜像，拉取失败直接停止，不影响旧容器运行
            echo "📥 开始拉取新镜像..."
            docker pull $IMAGE_REPO:latest
            
            # 2. 拉取成功后，停止并删除旧容器
            echo "🔄 停止并重启服务..."
            docker stop easynote || true
            docker rm easynote || true
            
            # 3. 启动新容器
            docker run -d \
              --name easynote \
              -p 8038:8000 \
              -v /aiModels/docker/EasyNote/data:/app/data \
              --env-file /aiModels/docker/EasyNote/.env \
              --restart unless-stopped \
              $IMAGE_REPO:latest
            
            # 4. 删除旧镜像释放空间
            docker image prune -f
            
            echo "✅ EasyNote 已基于新镜像部署成功！"

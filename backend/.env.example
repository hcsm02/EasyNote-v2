# EasyNote 后端环境变量配置示例
# 使用方法：复制此文件并重命名为 .env，然后填写具体的 API Key

# ==================== 1. 核心配置 ====================
# 数据库连接地址
# 默认使用项目根目录下的 SQLite 数据库
DATABASE_URL=sqlite:///./easynote.db

# 安全密钥（JWT 签名用）
# 建议在生产环境中使用随机的长字符串：openssl rand -hex 32
SECRET_KEY=your-super-secret-key-at-least-32-characters

# JWT 签名算法
ALGORITHM=HS256

# Token 有效期（分钟），默认 1440 分钟（24 小时）
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ==================== 2. 服务配置 ====================
# 是否开启调试模式（生产环境请设为 false）
DEBUG=true

# 跨域配置：允许访问后端的来源地址
# 多个地址请用逗号分隔，不要有空格
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==================== 3. AI 核心配置（多平台支持）====================
# 您可以根据拥有的 Key 选择填写，未使用的平台请保持为空

# Google Gemini (支持原生语音输入解析)
# 获取地址: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# 如果使用第三方代理，请取消下方注释并填写
# GEMINI_BASE_URL=https://api.apimart.ai
# GEMINI_MODEL=gemini-3-flash-preview

# 硅基流动 SiliconFlow (国内首选，支持多种开源大模型)
# 获取地址: https://cloud.siliconflow.cn
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# DeepSeek (极致性价比)
# 获取地址: https://platform.deepseek.com
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# OpenAI (行业标准)
# 获取地址: https://platform.openai.com
OPENAI_API_KEY=your_openai_api_key_here

# 默认 AI 服务商 (当用户在前端未指定时使用)
# 可选值: siliconflow, gemini, deepseek, openai
AI_DEFAULT_PROVIDER=siliconflow
